# 推理优化 · 自测

> **对应材料**：`llm_interview_note/06.推理/`

## KV Cache

- KV cache 为什么能加速自回归生成？存的是什么、省了什么重复计算？
- KV cache 的显存占用公式？为什么 MQA / GQA 能减小它？
- prefill 和 decode 阶段区别：为什么 prefill 是 compute-bound、decode 是 memory-bound？
- 为什么 decode 阶段 batch 大了吞吐会提升（算力换显存）？

## vLLM / PagedAttention

- PagedAttention 原理：为什么能减少显存碎片？和 OS 分页怎么类比？
- 为什么传统 KV cache 分配会浪费显存（internal / external fragmentation）？
- continuous batching 为什么比静态 batching 吞吐高？怎么处理不同长度请求的动态到达？
- vLLM 的调度器怎么在显存不够时做抢占 / 换出？

## 推理框架对比

- vLLM / TGI / TensorRT-LLM / FasterTransformer 各自定位和优势？
- 这些框架底层都依赖哪些核心技术（PagedAttention、FlashAttention、量化、kernel fusion）？
- 为什么 Triton / CUDA kernel 手写对推理加速很关键？

## 投机解码

- speculative decoding 原理：draft model + target model，为什么能加速？
- 接受 / 拒绝怎么判断（概率比较）？接受的 token 怎么保证分布正确？
- draft model 太大 / 太小各有什么问题？
- 投机解码和 MTP（Multi-Token Prediction）的关系与区别？

## 量化

- INT8 / INT4 量化的精度损失来源？
- GPTQ 和 AWQ 区别？为什么 AWQ 强调"保护重要权重"？
- 量化对 decode 阶段的加速为什么不如 prefill 明显（memory-bound）？
- KV cache 量化解决什么？为什么 KV cache 在长上下文下显存压力大？

## 采样参数

- Top-k / Top-p / Temperature 各自作用？为什么 temperature 高输出更随机？
- repetition penalty / presence penalty / frequency penalty 怎么实现？区别？
- 这些惩罚会不会破坏概率归一化？为什么？
- greedy / beam search / sampling 各适合什么场景？为什么 LLM 生成多不用 beam search？
