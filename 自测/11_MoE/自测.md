# MoE · 自测

> **对应材料**：`train_moe_from_scratch/moe_train.py`、`llm_interview_note/02.大语言模型架构/` 下 MoE 笔记

## 路由机制

- MoE 的 router/gate 怎么工作？输入是什么、输出是什么？
- top-k 选择中 k 通常取多少（1 / 2）？k=1 和 k=2 的 trade-off？
- router 的输出怎么变成 one-hot / soft 选择？为什么用 softmax + topk？
- 合上 `moe_train.py`，口述一个 MoE 层的 forward 流程（gate -> 选 expert -> 加权聚合）。

## 负载均衡

- 为什么需要 load balancing loss？不加会怎样（路由崩塌 / 热门 expert）？
- load balancing loss 公式？为什么用每个 expert 的平均 gate 概率和被选中的频率相乘？
- expert capacity（每个 expert 最多处理多少 token）解决什么？
- 被 capacity drop 的 token 怎么处理（丢弃 / 残差直通）？

## 训练稳定性

- MoE 训练的不稳定因素有哪些？
- 为什么 MoE 常需要辅助损失（z-loss / router 激活）？
- 为什么 MoE 对学习率更敏感？

## 设计权衡

- MoE 相比 dense：同等 FLOPs 下参数更多但激活更少，这个 trade-off 意味着什么？
- 细粒度 expert（更多更小的 expert）vs 粗粒度 expert，各有什么优劣？
- 共享 expert（DeepSeek 的设计）解决什么问题？
- Switch Transformer 相对早期 MoE 的改进？每 token 单 expert 的好处？

## 部署

- 推理时 MoE 怎么部署（expert 并行）？和 dense 推理区别？
- MoE 推理时每 token 只激活部分参数，为什么延迟不一定降低（调度 / 通信开销）？
- MoE + 并行：expert 怎么分布在多 GPU 上（EP）？和 TP/DP 怎么组合？
