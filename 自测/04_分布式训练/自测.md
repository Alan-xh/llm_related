# 分布式训练 · 自测

> **对应材料**：`llm_interview_note/04.分布式训练/`

## 显存与基础

- 训练时的显存由哪几部分构成？参数、梯度、优化器状态、activation 各占多少？
- 混精训练下 optimizer states（Adam）为什么比 fp32 还大？
- bf16 vs fp16 区别？为什么 bf16 更适合训练？fp16 为什么需要 loss scaling？

## 数据并行

- DP 和 DDP 区别？DDP 的梯度同步怎么和反向传播重叠？
- 数据并行的通信量是多少（和模型大小有关还是 batch 有关）？
- 梯度累加和 DDP 的 allreduce 怎么配合（什么时候触发同步）？

## ZeRO

- ZeRO-1/2/3 分别切分什么（optimizer states / gradient / params）？显存节省比例？
- 为什么 ZeRO-3 切 param 会带来额外通信开销？
- ZeRO 和 FSDP 的关系与区别？

## 张量并行

- 列切分（column-parallel）vs 行切分（row-parallel），前向 / 反向各怎么通信？
- 为什么 Megatron 里 attention 和 FFN 用不同的切分方式？
- 张量并行的通信量随 GPU 数怎么变化？为什么不适合跨多机？

## 流水线并行

- 流水线 bubble 是什么？怎么产生的？
- GPipe 和 PipeDream（1F1B）的区别？1F1B 怎么减少显存？
- 怎么减少 bubble（micro-batch 数量、interleaved schedule）？

## 序列 / 混合并行

- 序列并行解决什么问题？为什么长序列时 activation memory 爆炸？
- 数据并行 + 张量并行 + 流水线并行通常怎么组合？各自作用在哪个维度？
- 混合并行里，通信组怎么划分（DP group / TP group / PP group）？

## 通信原语

- AllReduce / AllGather / ReduceScatter 各做什么？
- Ring AllReduce 的通信量为什么是 O(N) 而不是 O(N²)？
- 通信和计算怎么重叠（overlap）？哪些操作容易重叠、哪些不容易？

## DeepSpeed / 工具

- DeepSpeed 的 ZeRO Stage 和 offload（cpu/nvme）各解决什么？
- 3D 并行（DP+TP+PP）在实践中怎么选各维度的规模？
- 为什么大模型训练很少纯数据并行，而是混合并行？
