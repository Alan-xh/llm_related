# NLP 基础算法 · 自测

> **对应材料**：`nlp/`（`NER.py`、`word_segment/HMM.py`、`CRF.py`、`n-gram.py`、`n-shortest-path.py`、`word2vec.md`）
> **纪律**：代码题合上源码默写算法核心步骤。

## 分词

- HMM 分词的三个概率（初始 / 转移 / 发射）分别是什么？怎么从语料估计？
- Viterbi 解码流程？时间复杂度？
- HMM 的两个强假设（一阶马尔可夫、输出独立）各带来什么局限？
- CRF 相比 HMM 为什么更强（判别式 vs 生成式、可加任意特征、全局归一化）？
- CRF 的损失函数？为什么是"给定观测下标签序列的条件概率"取负对数？
- n-shortest-path 分词原理？和最大匹配法、HMM 的区别？
- n-gram 语言模型为什么要平滑？Add-k / Good-Turing / Kneser-Ney 各自思路？
- 合上 `HMM.py` 默写 Viterbi 主循环；合上 `CRF.py` 口述特征函数和概率计算。

## NER

- NER 的 BIO / BIOES 标注体系？为什么需要 I-（Inside）标签？
- 转移约束（O 不能直接接 I-）怎么在解码时强制？
- NER 为什么本质是序列标注而非分类？
- 合上 `NER.py`，口述你的 NER 方案（特征 / 模型 / 解码）。

## Word2Vec

- CBOW 和 Skip-gram 区别？哪个适合小数据 / 高频词，哪个适合大数据 / 低频词？
- 为什么 Skip-gram 对低频词更友好？
- 负采样解决什么问题？为什么用 sigmoid 近似 softmax？
- 层次 softmax 原理？相比负采样 trade-off？
- word2vec 训练完后，为什么词向量能做类比（king - man + woman ≈ queen）？这反映了什么线性结构？

## 传统 vs LLM

- 传统 NLP pipeline（分词 -> 词性 -> NER -> 句法 -> 语义）和 LLM 端到端的根本区别？
- 传统方法在哪些场景仍有优势（可控、低资源、规则强）？
- 为什么 LLM 出现后，分词 / NER 作为独立任务的意义变弱了？
