# 强化学习与对齐 · 自测

> **对应材料**：`llm_interview_note/07.强化学习/`、`ppo_from_scratch/`、`s1_from_scratch/`

## 策略梯度基础

- 默写 REINFORCE 的策略梯度推导（从 log-likelihood 梯度出发）。
- 为什么需要 baseline？优势函数 A = R - b 怎么降低方差？
- on-policy vs off-policy 区别？PPO 属于哪种？ratio 为什么能做轻微 off-policy 修正？

## PPO

- 默写 PPO 的 clipped surrogate objective 公式，解释 ratio、ε 各是什么。
- ratio 为什么用 exp(logπ_new - logπ_old) 算，而不直接用 π_new / π_old？
- clip 范围 [1-ε, 1+ε] 的 ε 通常取多少？太大 / 太小各有什么后果？
- PPO-clip 和 PPO-penalty 两种变体区别？实际中为什么常用 clip？
- value function 和 advantage：为什么 PPO 需要 Critic？GAE 怎么平衡 bias 和 variance？
- 合上 `ppo_from_scratch/ppo_train.py`，口述一个完整 PPO 训练 step 的流程（采样→算 advantage→clip loss→更新）。

## RLHF

- RLHF 三阶段：SFT → RM → PPO，各阶段目标是什么？
- Reward Model 怎么训练？pairwise loss（Bradley-Terry）公式？
- 为什么 PPO 阶段要加 KL 惩罚（防止偏离参考模型）？不加会怎样（reward hacking / 分布外）？
- reward hacking 是什么？怎么缓解？
- RLHF 和 SFT 的根本区别（模仿 vs 优化偏好）？

## DPO

- 从 RLHF 目标推导 DPO loss，为什么能绕过 RM 直接从偏好数据优化？
- DPO 为什么不需要显式 reward model？reward 隐含在哪里？
- DPO 的偏好数据和 RLHF 的 RM 训练数据有何不同？
- DPO 相比 PPO 的优势（稳定、无 critic、无 KL 在线采样）和劣势？

## GRPO / DeepSeek

- GRPO 相比 PPO 去掉了什么？为什么用 group-relative advantage 而不用 value 网络？
- group-relative advantage 怎么算（同 prompt 采样多条，用组内 reward 归一化）？
- GRPO 省了 Critic 的显存和算力，代价是什么？

## s1 / 推理时缩放

- s1 的核心思想（test-time scaling / 等待 token / 预算强制）？
- 为什么简单的 RL + 长思考能提升推理能力？
- s1 用的数据量很小，为什么有效（高质量推理轨迹 vs 大规模）？
- 推理时 scaling 和训练时 scaling 是不是一回事？关系是什么？
