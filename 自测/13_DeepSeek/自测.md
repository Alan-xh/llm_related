# DeepSeek · 自测

> **对应材料**：`deepseek_learn/MLA.md`、`MLA.py`、`MTP_train/`、`deepseek_r1_train/`

## MLA（Multi-head Latent Attention）

- MLA 的核心：为什么能把 KV cache 压缩？latent 向量怎么替代 K、V？
- 默写 MLA 流程：输入 -> 下投影到 latent c -> 上投影出 K、V。各矩阵维度？
- MLA 的低秩压缩和 GQA / MQA 的本质区别？为什么 MLA 在长上下文更省？
- 推理解码时，MLA 为什么需要"吸收矩阵"把 W_UK 折进 W_Q？
- RoPE 和 MLA 怎么兼容（decoupled RoPE：把 RoPE 部分单独处理不和压缩 K 混）？为什么必须解耦？
- 合上 `MLA.py`，口述 MLA forward 的 shape 变化。它和标准 MHA 的 KV cache 大小差多少？

## MTP（Multi-Token Prediction）

- MTP 目标：为什么一次预测多个 token 能提升训练效率 / 模型能力？
- MTP 训练时多个预测头怎么共享表征？和 next-token prediction 怎么结合？
- MTP 训练和推理的不对称：训练用多 head，推理时可丢弃额外 head？
- MTP 和 speculative decoding 的关系与区别（训练时 vs 推理时）？

## DeepSeek-R1

- R1-Zero 为什么可以不经过 SFT 冷启动，直接 RL 训练？为什么 R1（非 Zero）又加回 SFT？
- R1 的 RL 训练流程：reward 怎么设计（规则 reward，正确性）？为什么不用 RM？
- "顿悟时刻"（aha moment）指什么？涌现的反思 / 自我纠错能力？
- R1 的思考 token（`<think>`）怎么训练 / 怎么用？

## GRPO

- GRPO 相比 PPO 去掉了 Critic，为什么可行？
- group-relative advantage 怎么算（同 prompt 采样 G 条，用组内 reward 归一）？
- GRPO 省了 Critic 的显存和算力，代价是什么（采样 G 条的开销）？
- GRPO 里还需要 KL 惩罚吗？为什么？

## DeepSeek-V3 MoE

- DeepSeek 的细粒度 expert + 共享 expert 设计，共享 expert 解决什么？
- DeepSeek 的 MoE 怎么做无辅助损失的负载均衡（偏置项调整）？
- MLA + MoE 组合，为什么对推理部署友好（低 KV cache + 稀疏激活）？
