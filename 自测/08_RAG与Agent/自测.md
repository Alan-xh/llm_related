# RAG 与 Agent · 自测

> **对应材料**：`llm_interview_note/08.检索增强rag/`

## RAG 基础

- RAG 相比 fine-tuning 的优势和劣势？什么场景该用 RAG、什么场景该微调？
- RAG 完整流程：chunking → embedding → retrieval → reranking → generation，每步的关键决策？
- 为什么需要 reranker？bi-encoder（双塔召回）vs cross-encoder（交叉重排）区别？

## 切分与检索

- chunk 大小怎么选？为什么需要重叠（overlap）？
- 向量检索：cosine vs L2 vs dot product？为什么 embedding 通常要归一化？
- 为什么 top-k 召回往往不够？hybrid search（稀疏 BM25 + 稠密向量）解决什么？
- 召回的难点：长 query、短 query、多义、OOV 实体，各怎么处理？

## 长上下文与 RAG

- 上下文窗口变长（128k+），为什么 RAG 仍然需要？
- "lost in the middle"现象是什么？怎么缓解？
- 检索回来的多个 chunk 怎么排序、去重、压缩？

## 生成与评估

- 检索到无关内容时，模型怎么知道"不答"（faithfulness）？
- 怎么评估一个 RAG 系统（faithfulness / answer relevance / context recall / precision）？
- RAG 的幻觉和纯生成幻觉，来源有什么不同？

## Agent

- Agent 的核心组件：planning / memory / tool use / action loop 各是什么？
- ReAct 框架：Thought-Action-Observation 循环怎么运作？
- function calling 怎么让 LLM 调用工具？和 ReAct 的关系？
- Agent 的 memory 怎么管理（短期对话窗 / 长期向量库）？
- Agent 多步推理时，错误怎么传播？怎么设计 self-check / 回退？
- Agent 和 RAG 的关系（RAG 可看作单步检索 Agent）？
