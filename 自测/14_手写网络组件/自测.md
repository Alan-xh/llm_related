# 手写网络组件 · 自测

> **对应材料**：`handwrite_network/`（`gpt2.py`、`MLA.py`、`RMSNorm.py`、`RotaryEmbedding.py`）
> **纪律**：所有题目合上源码默写或对着空气讲，讲不清 shape 变化的算 `✗`。

## RMSNorm

- 合上代码默写 `RMSNorm.forward`：和 LayerNorm 差在哪一行？
- 为什么去掉 mean 减法仍然有效？
- 为什么要 `.float()` 提升、最后转回原 dtype？不提升会怎样？
- `torch.rsqrt(variance + eps)`：eps 防止什么？rsqrt 是平方根倒数。

## RotaryEmbedding

- 默写 `RotaryEmbedding.__init__`：`inv_freq` 怎么算？为什么是 `1/(10000^(2i/d))`？
- cos/sin cache 的 shape 是什么？为什么注册成 buffer 而不是每次算？
- `rotate_half` 的实现和几何意义？为什么是 `cat(-x2, x1)`？
- `apply_rotate_pos_emb`：cos/sin 为什么要 `unsqueeze(unsqueeze_dim)`？对 q 和 k 怎么施加旋转？
- 为什么只对 q、k 旋转，不对 v 旋转？（RoPE 通过 q·k 的相对位置起作用）

## MLA

- 合上 `MLA.py`，口述 forward：latent 向量 c 怎么经上投影得 K、V？压缩矩阵和解压矩阵的维度？
- MLA 的 KV cache 存的是什么（latent c 而非 K、V）？为什么能省？
- 吸收矩阵的原理：为什么推理时把 W_UK 折进 W_Q？
- MLA 和标准 MHA 的参数量、KV cache 大小对比？

## GPT（你的 gpt2.py 是空的 -- 用这题驱动补完）

- 合上代码默写一个完整的 GPT block（Pre-Norm + Causal Self-Attention + SwiGLU/MLP + 残差）。
- 默写 `CausalMultiHeadAttention.forward`，每步标注 shape：[B,L,C] -> [B,H,L,D] -> ...
- 因果掩码怎么构造？`torch.triu(ones, diagonal=1).bool()` 的 diagonal=1 是什么意思？
- 为什么手写时容易在 mask、transpose、contiguous().view() 上出错？这暴露了对什么理解不到位？
- 你能合上代码默写出 `train_llm_from_scratch/train.py` 里那个 Attention 类的 forward 吗？（包括 GQA 的 repeat_kv）

## 反思

- 手写 vs 调 `F.scaled_dot_product_attention`，前者让你多理解了什么？
- 哪些组件你写的时候是"照着抄"的、哪些是"理解了默写"的？前者就是下一步要补的。
