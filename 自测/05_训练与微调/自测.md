# 训练与微调 · 自测

> **对应材料**：`llm_interview_note/03.训练数据集/`、`llm_interview_note/05.有监督微调/`

## 预训练

- 预训练的 CLM loss 公式？为什么用 causal mask？
- 预训练数据清洗：去重、质量过滤、tokenize 各为什么重要？去重为什么能防"背答案"？
- learning rate warmup + cosine decay 为什么这么配？weight decay 作用？
- 预训练 vs 继续预训练（CPT）的区别？什么时候做 CPT？

## SFT

- SFT 数据格式：instruction / input / output 怎么组织？
- 为什么 SFT 只对 output 部分算 loss（mask 掉 prompt）？不 mask 会怎样？
- 多轮对话 SFT 的 loss mask 怎么处理（每轮 user 部分都不算？还是只算最后一轮）？
- 为什么 SFT 容易过拟合？学习率、epoch 通常怎么设？
- SFT 用全量微调还是 LoRA，怎么选？

## 参数高效微调

- LoRA 原理：W = W₀ + BA，为什么 B 零初始化、A 高斯初始化？
- LoRA 的 rank r 怎么选？r 太大 / 太小各有什么影响？
- LoRA 合并后推理为什么无额外开销？训练时为什么省显存？
- Adapter / Prefix-tuning / Prompt-tuning 各自思路？和 LoRA 区别？
- 全量微调 vs PEFT 的适用场景？为什么大模型微调倾向 PEFT？

## Prompting

- zero-shot / few-shot / chain-of-thought prompting 区别？
- few-shot 例子怎么选？顺序会影响结果吗？
- instruction tuning 和 prompt tuning 是一回事吗？区别？

## 训练细节

- 梯度裁剪（clip grad norm）解决什么？max_norm 通常设多少？
- 为什么用 AdamW 而非 Adam？weight decay 在 AdamW 里怎么解耦的？
- 学习率太大 / 太小的现象分别是什么？怎么诊断？
- 训练 loss 突然 spike（爆炸）可能的原因和处理？
- 模型并行 / 数据并行下，loss 怎么正确聚合？
