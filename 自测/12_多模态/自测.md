# 多模态 · 自测

> **对应材料**：`train_multimodal_from_scratch/`、`train_siglip_from_scratch/`

## CLIP / SigLIP

- CLIP 的双塔结构：image encoder + text encoder 输出怎么对齐？
- 默写 CLIP 的对比学习 loss（InfoNCE），对称形式是什么？
- 为什么 CLIP 的 loss 对称（image-to-text + text-to-image）？温度 τ 的作用？
- CLIP 的 zero-shot 分类怎么做的（用文本 prompt 编码类别）？
- SigLIP 相比 CLIP 改了什么（sigmoid vs softmax）？为什么 sigmoid 不需要全局归一化、更适合大 batch？

## 对比学习训练

- 为什么对比学习需要大 batch / 多负样本？
- SigLIP 训练时 batch size 和负样本数量的关系？为什么小 batch 也能训（sigmoid 是逐对的）？
- 合上 `train_siglip_from_scratch/train.py`，口述一个 batch 的 loss 计算流程。

## VLM 视觉语言模型

- VLM 把视觉特征接入 LLM 的三种范式：投影式（LLaVA）/ 交叉注意力式（Flamingo）/ Q-Former（BLIP-2），各自怎么接？
- LLaVA 的训练为什么分两阶段（stage1 投影层对齐 -> stage2 指令微调）？
- 合上 `train_multimodal_from_scratch/train.py`，口述多模态怎么把图像 token 和文本 token 拼接输入 LLM。
- 图像 token 化：ViT 的 patch embedding 怎么做？为什么 16×16 patch？

## 难点

- 多模态对齐的难点：模态 gap、分辨率、位置信息怎么处理？
- 多模态指令数据怎么构造（image + instruction + response）？
- 高分辨率图像怎么处理（切分成多个子图 / token 压缩）？
- 多模态评测和纯文本评测的区别（需要 image-text pair）？
