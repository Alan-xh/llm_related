# LLM 基础 · 自测

> **对应材料**：`llm_interview_note/01.大语言模型基础/`、`nlp/word2vec.md`

## 分词

- BPE / WordPiece / SentencePiece 三者区别？为什么主流 LLM 多用 BPE？
- BPE 的训练流程：从字符级开始，怎么合并、合并依据是什么？
- 为什么中文需要分词，而现代 LLM 用 BPE 后"分词"概念变弱了？
- jieba 分词的原理（前缀词典 + HMM）？和 BPE 的本质区别在哪？

## 词向量

- 词向量从 one-hot → Word2Vec → 上下文向量（ELMo/BERT）的演进，每一步解决了什么？
- Word2Vec 的 CBOW 和 Skip-gram 区别？哪个适合低频词、为什么？
- 负采样解决什么问题？为什么用 sigmoid 近似 softmax？
- 层次 softmax 的原理？相比负采样 trade-off？

## 语言模型

- n-gram 语言模型的根本局限是什么？平滑（Add-k / Good-Turing / Kneser-Ney）为什么需要？
- 神经网络语言模型相对 n-gram 解决了什么（泛化、维度灾难）？
- 自回归语言模型的损失函数公式？为什么用 causal mask？

## 激活函数

- Sigmoid / Tanh 为什么会导致梯度消失（从导数范围分析）？
- ReLU 相比 Sigmoid 的优势？ReLU 的 dead neuron 问题怎么缓解（LeakyReLU/GeLU）？
- GELU 的设计动机？为什么 BERT/GPT 用 GELU？
- SwiGLU 相比 GELU FFN 多了什么？为什么现代 LLM 用它？

## 架构选型

- 为什么 LLM 普遍用 Decoder-only 而非 Encoder-Decoder？从双向 vs 单向、统一生成、scaling 友好性三方面分析。
- Encoder-only（BERT）适合什么任务？为什么不适合开放生成？
- NLP 三大特征抽取器 CNN / RNN / Transformer 各自优劣？为什么 Transformer 胜出？
