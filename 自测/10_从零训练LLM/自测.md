# 从零训练 LLM · 自测

> **对应材料**：`train_llm_from_scratch/train.py`、`sft_train.py`、`dpo_train.py`、`dataset.py`
> **代码题纪律**：合上源码口述或默写，能讲清每步 shape 变化才算通过。

## RoPE

- 你代码里 cos/sin cache 为什么预计算并注册成 buffer？每次 forward 重算会怎样？
- `apply_rotate_pos_emb` 里 `rotate_half` 的几何意义？为什么是 `(-x2, x1)`？
- 为什么 RoPE 对 q、k 旋转而对 v 不旋转？
- 你的 `RotaryEmbedding` 里 `inv_freq = 1/(10000^(2i/d))`，默写这个的来历。

## RMSNorm

- 你的 `RMSNorm.forward` 里和 LayerNorm 差在哪一行？为什么去掉了 mean 减法？
- 为什么要先 `.float()` 再算、最后再转回？不转会怎样（数值精度）？
- `torch.rsqrt(variance + eps)` 里 eps 的作用？

## Attention / GQA

- `repeat_kv` 做了什么？为什么 KV head 少了要 expand？用 view+reshape 还是 repeat，为什么？
- GQA 的 KV cache 大小相比 MHA 小多少？公式是什么？
- 你的 flash_attn 分支用 `F.scaled_dot_product_attention`，它相比手写循环好在哪？

## ⚠️ 代码 bug 复现（先答再翻代码核对）

- 第 97 行 `if use_kv_cache and self.eval():` 这个判断有什么问题？`self.eval()` 返回什么？本意应该怎么写？
- 第 134 行 `scores + self.mask[:, :, :s, :s]` 里 `self.mask` 从哪来？为什么默认走 flash_attn 就没暴露这个 bug？
- 这两个 bug 暴露了什么理解盲区？怎么改才对？

## MLP / 整体

- SwiGLU 的 `down_proj(silu(gate_proj(x)) * up_proj(x))`：为什么 gate 和 up 分两个线性层？相比 GELU FFN 参数多了怎么补偿？
- weight tying（`tokon_embeddings.weight = output.weight`）为什么有效？有什么潜在问题（梯度耦合）？
- 你的 init 里为什么部分层用 `std = 0.02 / sqrt(2 * n_layers)` 缩放？残差路径缩放解决什么？

## generate

- `generate` 里 repetition penalty 为什么是 `logits[:, token] /= penalty`？这会破坏概率归一化吗？
- top_k 采样时 `logits[logits < v[:, [-1]]] = -inf` 的 v 是什么？为什么取 `[:, -1]`？
- KV cache 的 generate 逻辑：为什么增量输入只取最后一个 token？你的 cache 实现对吗（结合上面的 bug）？

## 训练流程

- `TrainingArguments` 里 `gradient_accumulation_steps=8` + `per_device_train_batch_size=128`，有效 batch 是多少？为什么需要累积？
- `bf16=True` 和 `save_safetensors=False` 各是什么含义？
- DPO 训练和 SFT 训练在代码上的主要区别（数据格式、loss、reference model）？翻 `dpo_train.py` 之前先答。
- tokenizer 训练：BPE 训练流程？vocab_size 怎么定？为什么需要 special tokens？
