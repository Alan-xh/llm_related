# LLM 架构 · 自测

> **对应材料**：`llm_interview_note/02.大语言模型架构/`

## Attention

- 默写 scaled dot-product attention 公式，解释每个符号。
- 为什么除以 √d_k？不除会怎样（从 softmax 饱和、梯度角度）？
- 多头注意力相比单头的好处？head 数太多 / 太少各有什么问题？
- 为什么 attention 天然支持变长序列？padding mask 和 causal mask 区别？

## 位置编码

- 绝对位置编码 vs 相对位置编码 vs RoPE，各自怎么编码位置信息？
- RoPE 怎么把"绝对位置"变成"相对位置"的？为什么对 q、k 旋转而对 v 不旋转？
- 默写 RoPE 的旋转矩阵推导：inv_freq 为什么是 10000^(-2i/d)？
- RoPE 的长文本外推问题怎么解决（NTK-aware / YaRN / PI）？

## Normalization

- BatchNorm vs LayerNorm：为什么 NLP 用 LayerNorm 不用 BatchNorm？
- LayerNorm vs RMSNorm：RMSNorm 去掉了什么？为什么可以去掉？
- Pre-Norm vs Post-Norm：训练稳定性差异？为什么现代 LLM 用 Pre-Norm？
- Norm 放在残差连接里还是外，对梯度流有什么影响？

## 注意力变体

- MHA → MQA → GQA 演进：各自的 KV cache 大小？为什么 GQA 是折中？
- MQA / GQA 的推理加速主要来自哪？对质量的影响？
- KV cache 原理：为什么推理时存 K、V 能加速？显存占用怎么算（layers × 2 × seq × kv_heads × head_dim）？

## MoE

- MoE 的 router/gate 怎么工作？top-k 选择中 k 通常取多少？
- 为什么需要 load balancing loss？不加会怎样（路由崩塌）？
- expert capacity 限制解决什么？被 drop 的 token 怎么办？
- MoE 相比 dense：同等 FLOPs 下参数更多但激活更少，trade-off 是什么？

## 代表模型

- LLaMA 的架构选择：Pre-Norm + RMSNorm + SwiGLU + RoPE + GQA，每一项相对原始 Transformer 改了什么？
- BERT 的 [CLS] token 怎么用于分类？MLM 为什么 mask 15% 且按 80/10/10 拆分？
- llama2 / llama3 相比 llama1 的主要变化（上下文长度、GQA、训练数据）？
- Transformer 架构里Decoder-Only的交叉注意力去哪了？为什么不需要？

## 解码

- Top-k / Top-p / Temperature 各自作用？为什么 temperature 高输出更随机？
- Top-p（nucleus）相比 Top-k 的优势？
- repetition penalty 怎么实现？会不会破坏概率分布？
- 为什么 decode 阶段是 memory-bound 而 prefill 是 compute-bound？
