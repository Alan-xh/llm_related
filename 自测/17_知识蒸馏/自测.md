# 知识蒸馏 · 自测

> **对应材料**：`knowledge_distillation_llm/`（`train.py`、`dataset.py`、`utils.py`）

## 蒸馏原理

- 知识蒸馏的 KL loss 为什么用 soft target（带 temperature T）？
- temperature T 怎么选？T 太大 / 太小各有什么影响？
- 为什么 soft label 比 hard label 信息更多（dark knowledge：类间相似度）？
- 蒸馏 loss 通常怎么组合（KL soft + CE hard）？两项权重怎么调？

## Teacher / Student

- teacher 和 student 容量差距太大 / 太小各有什么问题？
- teacher 必须比 student 强吗？自蒸馏（self-distillation）为什么也有效？
- teacher 怎么选择 / 训练？

## 蒸馏方式

- logit 匹配（输出层）vs 中间特征匹配（hidden state）vs attention 匹配，各自适用场景？
- 特征匹配为什么要对齐维度（投影层）？
- 序列级蒸馏（teacher 生成数据给 student 训练）和 logit 蒸馏区别？

## LLM 蒸馏特殊问题

- LLM 蒸馏相比 CV 蒸馏的特殊挑战（自回归、序列级 loss、生成质量）？
- 合上 `train.py`，口述你的蒸馏 loss 怎么算（teacher logits vs student logits 的 KL）。
- 蒸馏时 teacher 的 forward 要不要梯度？为什么（no_grad）？
- 蒸馏和 RLHF / DPO 能结合吗？思路是什么？

## 与其他压缩技术

- 蒸馏 vs 量化 vs 剪枝，各自压缩的是什么？
- 它们能组合吗（蒸馏到小模型再量化）？组合顺序有讲究吗？
- 蒸馏得到的 student 在部署上有什么优势？
